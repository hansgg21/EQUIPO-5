package gui;

import javax.swing.*;
import java.awt.*;

public class MiddlePanel extends JPanel {
    public MiddlePanel() {
        setLayout(new GridLayout(3, 1, 10, 10));  // Tres botones en una columna
        // Agrega los botones aquí en el constructor o en un método separado
    }
}
